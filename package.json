{
  "name": "mockup-vision",
  "version": "0.6.0",
  "private": true,
  "type": "module",
  "description": "Guided photo-first mockup studio with Cloudflare-first scene generation, multi-slot artwork placement and a preserved cylindrical vision lab.",
  "scripts": {
    "start": "node server/index.js",
    "dev": "node server/index.js",
    "check": "node --check studio-app.js && node --check studio-ux.js && node --check studio-api-monitor.js && node --check src/studio-core.js && node --check src/scene-brief.js && node --check src/planar-core.js && node --check server/index.js && node --check server/cloudflare-provider.js && node --check server/openai-provider.js && node --check photo-app.js && node --check scene-builder.js",
    "test": "node --test tests/*.test.js",
    "ci": "npm run check && npm test"
  },
  "engines": {
    "node": ">=24"
  }
}
