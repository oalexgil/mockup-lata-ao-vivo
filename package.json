{
  "name": "mockup-vision",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "Photo-first mockup studio with scene briefing, planar perspective fitting and a preserved cylindrical vision lab.",
  "scripts": {
    "check": "node --check scene-builder.js && node --check photo-app.js && node --check src/scene-brief.js && node --check src/planar-core.js",
    "test": "node --test tests/*.test.js",
    "ci": "npm run check && npm test"
  },
  "engines": {
    "node": ">=24"
  }
}
